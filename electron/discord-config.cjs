/**
 * Discord Rich Presence uses one Application ID for the whole Stellar build.
 * Create an app at https://discord.com/developers/applications → OAuth2 → Client ID,
 * paste it here once, rebuild — users never have to type an ID in the UI.
 * Leave empty to skip RPC until configured.
 */
module.exports = {
  clientId: '',
}
