/**
 * Query-client stub.
 * @tanstack/react-query is not installed — this prevents import errors.
 * Install the package and restore the real QueryClient when ready.
 */
export const queryClientInstance = null;